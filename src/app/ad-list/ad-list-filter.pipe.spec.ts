import { AdListFilterPipe } from './ad-list-filter.pipe';

describe('AdListFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AdListFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
